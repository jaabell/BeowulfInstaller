---
  - name: "Setting up python environment"
    hosts: compute

    tasks:

      - name: Install python3 is python with apt
        apt:
        name: python-is-python3

      - name: Install python3 distutils
        apt:
        name: python3-distutils

      - name: Install python3 pip
        apt:
        name: python3-pip
